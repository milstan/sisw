source "https://rubygems.org"

# Pins us to whatever Jekyll + plugins GitHub Pages currently runs,
# so local previews (`bundle exec jekyll serve`) match production.
gem "github-pages", group: :jekyll_plugins
